@model IEnumerable<WebApplication4.Models.MetotRol>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Yeni oluştur", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Metot.Ad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rol.Ad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Metot.Ad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rol.Ad)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.No }) |
       
           
        </td>
    </tr>
}

</table>
